# 基础
torch>=2.0
transformers>=4.35
datasets>=2.14
accelerate>=0.24
peft>=0.6
bitsandbytes>=0.41

# API
fastapi
uvicorn[standard]
gunicorn

# 模型下载
modelscope

# 推理加速
vllm
# flash-attn requires GPU + pre-installed torch; install manually: pip install flash-attn --no-build-isolation

# 工具
pyyaml
pydantic

# 测试
httpx
pytest

# 量化
auto-gptq>=0.7
optimum>=1.16
